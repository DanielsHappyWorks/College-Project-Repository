/*
public class exercise {
	public static void main(String[] args){
		exercise theApp = new exercise();
		theApp.start();
	}
	public void start(){
		int x =15;
		int y = 3;
		boolean bSet = isSet(x,y);
		System.out.println("bSet: "+ bSet);
	}
	public boolean isSet(int value, int position){//returns true if a bits position is set (equals to 1)
		int mask = 1 << position;
		int result = value & mask;
		return (result > 0);//can use if statement for return
	}
}
*/