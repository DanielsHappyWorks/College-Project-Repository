#include <iostream>

using namespace std;

int main(){
	//create hotel with vectors
	//use if to go through all of the options using an integer
	//place right method inside of each if statement

	system("Pause");
	return 0;
}